We would focus on two aspects in the future, the potential practical applications  of {\THESYSTEM} and how to improve the
accuracy of {\THESYSTEM}.
%
% We plan to investigate the potential integration of {\THESYSTEM} in an adaptive data analysis framework like Guess and check by Rogers at al.~\cite{RogersRSSTW20}. As we discussed, this framework is designed to support adaptive data analyses with limited generalization error. As our experiments show, this framework could benefit from the information provided by {\THESYSTEM} to provide a more precise estimation and improved confidence intervals. Another direction we will explore is to make 
% the upper bounds provided by {\THESYSTEM} are more precise by integrating our algorithm with a path-sensitive approach.

\subsubsection{Practical Applications}
\label{sec:future-adapt-application}
We plan to investigate the potential integration of {\THESYSTEM} in an adaptive data analysis framework like Guess and check by Rogers at al.~\cite{RogersRSSTW20}. As we discussed, this framework is designed to support adaptive data analyses with limited generalization error. As our experiments show, this framework could benefit from the information provided by {\THESYSTEM} to provide a more precise estimation and improved confidence intervals. 

\subsubsection{Accuracy}
\label{sec:future-adapt-accuracy}
Another direction we will explore is to make 
the upper bounds provided by {\THESYSTEM} are more precise by integrating our algorithm with a path-sensitive approach.
Specifically, we plan to improve the accuracy from the following two aspects.

\paragraph{The Adaptivity Definition.}
The adaptivity definition we give in Definition~\ref{def:trace_adapt}
is not precise enough with respect to the intuitive \emph{adaptivity} notion.
There are  some cases in which the definition over-approximate the intuitive \emph{adaptivity} of the programs.
The Example~\ref{ex:multiRoundsS} in Section~\ref{sec:adapt-example} shows one example of this over-approximation.
% It is 
In this example, the adaptivity by semantics-based definition in Section~\ref{sec:adapt-exe} 
is $\lambda \trace_0 \sthat \env(\trace_0) k$, i.e., the initial variable of the input variable.
However, the intuitive \emph{adaptivity} is only $2$ given whatever initial trace, i.e., $\lambda \trace_0 \sthat 2$.
This over-approximation is resulted from the
% difference 
irrelevance between the 
% dependency Depth 
weight
%  (i.e., the vertex weight) 
and 
% the data dependency relation (the edge) in
the \emph{variable may-dependency} relation.
%  in Section~\ref{sec:dynamic-datadep}.
Specifically the irrelevance between the vertex weight and the edge on the \emph{semantics-based dependency} graph.
When the 
% weight 
% dependency quantity
weight of a vertex is counted from the execution trace that is different from the witness traces used in 
% witness 
% analyzing 
the \emph{variable may-dependency} relation of that vertex, there is an inaccuracy.

% \paragraph{Proposed Methodology}
% \label{para:furthers-dep-depth}

Based on the \emph{adaptivity} formalization in Section~\ref{sec:adapt-exe}, the improvement on the accuracy 
of
\emph{adaptivity} definition is planned to develop in the following steps.
\begin{enumerate}
\item In the first stage of formalization, 
I will give an alternative \emph{variable may-dependency} definition 
by referring to the methodology in~\cite{Cousot19a}.
%
Specifically, I will define the variables' dependency relation over two witness traces and an initial trace. Comparing to 
the existing \emph{variable may-dependency} definition, which quantifies overall possible execution traces, the alternative
the definition explicitly relies on two specific witness traces from the program executions.
This externalization helps in analyzing the \emph{dependency quantity} based on the same 
witness traces as the \emph{variable may-dependency} relation. In this way, the over-approximation as illustrated above
can be reduced.
%
\item Then based on the new \emph{variable may-dependency} definition,
I will add the weight of every edge constructed from 
\emph{variable may-dependency} relation in the \emph{semantics-based dependency} graph, with respect to to the witness traces.
%
\item Then in the third stage, the \emph{adaptivity} is still defined as the 
length of the longest finite walk. Differently, the finite walk is added
with extra restriction.
The occurrence of every edge in a finite walk is restricted to be no more than its weight as well.
\end{enumerate}
Through these steps above, ideally, we will give a more accurate definition of the intuitive \emph{adaptivity}.

\paragraph{Adaptivity Static Analysis.}
% \label{sec:future-adaptfun}
In the adaptivity analysis algorithm $\THESYSTEM$,
%  specifically on the dependency quantity, 
we use the reachability bound analysis technique to estimate the weight for each
labeled variable.
%  dependency quantity.
% In existing static reachability bound analysis, 
However,
it comes across an over-approximation due to its path-insensitive nature.
Specifically, it is not precise enough with respect to the semantics-based weight for the labeled
variables in different loop paths.
The Example~\ref{ex:multiRoundsO} shows this limitation.
% It is 
In this example, the estimated adaptivity by static analysis in Section~\ref{sec:adapt-static} 
is $1 + 2*k$ where $k$ is the input variable.
However, the formal \emph{adaptivity} is only $1 + k$ from the semantics-based analysis.
This is because that the weight estimation algorithm in Section~\ref{sec:alg_weightgen} is path-insensitive.
It occurs when the control flow can be decided in a particular way in front of conditional branches, 
while the static analysis fails to witness. 

The accurate static adaptivity analysis is planned to be improved by using our \emph{path-sensitive reachability-bound} algorithm
from \redd{PART \romannum{2} } as follows.
\begin{itemize}
 \item New $\THESYSTEM$ will first construct the abstract control flow graphs with extended constraints
%   based on the requirement of the newly-designed
 according to the \emph{path-sensitive reachability bound} algorithm.

 \item We will still perform the same algorithm for analyzing the dependency relations.
 \item Then in the second step, we improve the $\THESYSTEM$ by
 performing the newly-designed \emph{path-sensitive reachability bound} algorithm 
 on each program location for estimating its visiting times during the program execution.
%   of each labeled variable.
%   the dependency quantity.
 The result from the new algorithm 
 gives us a tighter approximation
 on
%   of the dependency quantity,
%  specifically on 
the weight of each labeled variable.
 \item At the end, we still construct the same \emph{estimated dependency graph}
 but using more accurate estimated result, and compute a tighter \emph{adaptivity} for the programs.
%  Based on the two results analyzed from the steps above, 
%  i.e., the estimated dependency relation and the tighter reachability bound, the improved $\THESYSTEM$ will construct
% a more accurate program-based data dependency graph.
% Then, by applying Algorithm~\ref{alg:adapt} in Section~\ref{sec:alg_adaptcompute}, 
% the improved $\THESYSTEM$ will compute a tighter \emph{adaptivity} for a program.%
\end{itemize}
% \end{enumerate}
Again as shown in Figure~\ref{fig:multiRoundsO}(b), ideally we will compute $\frac{k}{2}$ as the weight for vertices $p^6$ and $x^7$ through the newly-designed path-sensitive reachability bound analysis algorithm!
This also gives the motivation for the work in \redd{ PART \romannum{2}} in this thesis.

