We first introduce some conventional operators as follows, which will be used in the proof.
\begin{defn}[Subtrace]
  Subtrace: $[ : ] : \mathcal{{T} \to \eventset \to \eventset \to \ftdom}$ 
  \[
    \trace[\event_x : \event_y] \triangleq
    \left\{
    \begin{array}{ll} 
    \trace'[\event_x: \event_y]             & \trace = \event :: \trace' \land \event \eventneq \event_x \\
    \event_x :: \trace'[:\event_y]  & \trace = \event :: \trace' \land \event \eventeq \event_x \\
    {[]} & \trace = [] \\
    \end{array}
    \right.
  \]
  For any trace $\trace$ and two events $\event_x, \event_y \in \eventset$,
  $\trace[\event_x : \event_y]$ takes the subtrace of $\trace$ starting with $\event_x$ and ending with $\event_y$ including $\event_x$ and $\event_y$.
  \\
  We use $\trace[:\event_y] $ as the shorthand of subtrace starting from head and ending with $\event_y$, and similary for $\trace[\event_x:]$.
  \[
    \trace[:\event] \triangleq
    \left\{
    \begin{array}{ll} 
   \event' :: \trace'[: \event]             & \trace = \event' :: \trace' \land \event' \eventneq \event \\
    \event'  & \trace = \event' :: \trace' \land \event' \eventeq \event \\
    {[]}  & \trace = [] 
    \end{array}
    \right.
  % \]
  % \[
    \quad
    \trace[\event: ] \triangleq
    \left\{
    \begin{array}{ll} 
    \trace'[\event: ]     & \trace =  \event' :: \trace' \land \event \eventneq \event' \\
    \event' :: \trace'  & \trace = \event' :: \trace' \land \event \eventeq \event' \\
    {[ ] } & \trace = []
    \end{array}
    \right.
  \]
\end{defn}
%
Program Entry Point: $\entry(c) : \mbox{Command} \to \mathbb{N}$ 
\[
  \entry(c) \triangleq 
\left\{
  \begin{array}{ll} 
     l       
    & c = [\eskip]{}^l
    \\ 
    l    & c = [\assign{x}{\expr_1}]{}^l
    \\ 
    l      
    & c = \clabel{\assign{x}{\query(\qexpr_1)}}{}^l
    \\
   l
    & c_1 = \eif([b]{}^l, c_t, c_f)
    \\ 
    l         
    & c = \ewhile [b]^l \edo (c_w)
    \\ 
    \entry({c1})
    & c = c1;c2
  \end{array}
  \right.
\]
The \emph{Latest Label}
operator $\llabel : \ftdom \to \mathcal{V} \to \mathbb{N} \cup \{ \bot\}$,
takes a trace and a variable as input and returns the label of the latest assignment event which assigns value to that variable. 
  Its behavior is defined as follows,
  \begin{defn}[Latest Label]
    \label{def:latest_label}
    Given the trace $\trace \in \tdom$ and a variable $x \in \mathcal{VAR}$,
    the \emph{latest label} of $x$ is computed as follows.
    \[
      \begin{array}{l}
    \llabel(\trace  :: (x, l, \_, \_)) x \triangleq l
    \qquad~
    \llabel(\trace  :: (y, l, \_, \_)) x \triangleq \llabel(\trace ) x, y \neq x
    % &
    \\
    \llabel(\trace :: (b, l, v, \bullet)) x \triangleq \llabel(\trace) x
    \qquad~
    \llabel({[]}) x \triangleq \bot
    \end{array}
    \]
  \end{defn}
  %

  \begin{thm}[$\eventdep$ implies $\flowsto$]
    \label{thm:flowsto_event_soundness}
    For every $D \in \dbdom , c \in \cdom, \trace \in \ftdom \sthat  \forall \event_x, \event_y \in \eventset \sthat 
    \event_x, \event_y \in \eventset^{\asn}$, 
    if $\exists \trace' \in \ftdom \sthat  \trace = [\event_x] \tracecat \trace' \tracecat [\event_y]$ and 
    $\eventdep(\event_x, \event_y, \trace, c, D) $, then
    there exists $z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c)$ with $n \geq 0$ such that   
    $\flowsto(x^i,  z_1^{r_1}, c) 
    \land \ldots \land \flowsto(z_n^{r_n}, y^j, c)$
    %
    \[
      \begin{array}{l}
        \forall D \in \dbdom , c \in \cdom, \trace \in \ftdom, \event_x, \event_y \in \eventset, x^i, y^j \in \lvar(c) \sthat 
        \\ \qquad 
        \pi_1(\event_x)^{\pi_2(\event_x)} = x^i
        \land 
        \pi_1(\event_y)^{\pi_2(\event_y)} = y^j
      \land 
        %  \exists \trace' \in \ftdom \sthat  \trace = [\event_x] \tracecat \trace' \tracecat [\event_y]
        % \\ \qquad 
        % \implies
        \eventdep(\event_x, \event_y, \trace, c, D) 
        \\ \quad 
        \implies 
        \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat 
        \\ \qquad 
         n \geq 0 \land
        \flowsto(x^i,  z_1^{r_1}, c) 
        \land \ldots \land \flowsto(z_n^{r_n},  y^j, c) 
      \end{array}
    \]
    \end{thm}
  
%
% \begin{thm}[$\eventdep$ implies $\flowsto$]
% \label{thm:flowsto_event_soundness}
% For every $D \in \dbdom , c \in \cdom, \trace \in \ftdom \sthat  \forall \event_x, \event_y \in \eventset \sthat 
% \event_x, \event_y \in \eventset^{\asn}$, 
% if $\exists \trace' \in \ftdom \sthat  \trace = [\event_x] \tracecat \trace' \tracecat [\event_y]$ and 
% $\eventdep(\event_x, \event_y, \trace, c, D) $, then
% there exists $z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c)$ with $n \geq 0$ such that   
% $\flowsto(x^i,  z_1^{r_1}, c) 
% \land \ldots \land \flowsto(z_n^{r_n}, y^j, c)$
% %
% \[
%   \begin{array}{l}
% 		\forall D \in \dbdom , c \in \cdom, \trace \in \ftdom \sthat  \forall \event_x, \event_y \in \eventset \sthat 
% 		\\ \qquad 
%     \event_x, \event_y \in \eventset^{\asn} \land 
% 		 \exists \trace' \in \ftdom \sthat  \trace = [\event_x] \tracecat \trace' \tracecat [\event_y]
% 		\\ \qquad 
%     \implies
% 		\eventdep(\event_x, \event_y, \trace, c, D) 
% 		\\ \quad 
% 		\implies 
%     \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat 
%     \\ \qquad 
%      n \geq 0 \land
% 		\flowsto(\pi_1(\event_x)^{\pi_2(\event_x)},  z_1^{r_1}, c) 
%     \land \ldots \land \flowsto(z_n^{r_n}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) 
% 	\end{array}
% \]
% \end{thm}
% This proof is summarized informally as below.
% \\
% I. Vacuously True cases, where trace doesn't satisfy the hypothesis 
% \\
% II. Base case where $\trace = [\event_x;\event_y]$
% \\
% III. inductive case where $\trace = [\event_x, \ldots, \event_y]$.
% \\
% 1. Existence of a middle event:
% \\
% Proved by showing a contradiction, with detail in Lemma~\ref{lem:depevents_exist}.
% %
% \\
% 2. The middle event with a sub-trace implies a "sub flowsto-chain", informally:
% %
% \\
% (1). obtaining the existence of $\event_z \in \eventset^{\asn} $ with dependency on $\event_x$, and a "direct flowsto" from $\event_z$ to $\event_y$
% by Lemma~\ref{lem:depevents_exist}.
%  \\
% (2). from the dependency of the $\event_z$ with $\event_x$ on the subtrace,
%  obtaining a "sub flowsto-chain" by induction  hypothesis;
%  \\
% (3). composing the "sub flowsto-chain" from (2) with the  "direct flowsto" from (1), and getting the conclusion of
%  the complete "flowsto chain".
% %
\begin{proof}

  % By Definition~\ref{def:event_dep}, we know 
  Taking arbitrary $D \in \dbdom , c \in \cdom, \trace \in \ftdom, \event_x, \event_y \in \eventset, x^i, y^j \in \lvar(c)$
  such that
  \[
  \pi_1(\event_x)^{\pi_2(\event_x)} = x^i
  \land 
  \pi_1(\event_y)^{\pi_2(\event_y)} = y^j
\land 
  %  \exists \trace' \in \ftdom \sthat  \trace = [\event_x] \tracecat \trace' \tracecat [\event_y]
  % \\ \qquad 
  % \implies
  \eventdep(\event_x, \event_y, \trace, c, D).\]
  Then it is sufficient to show
  \[
    \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat 
         n \geq 0 \land
        \flowsto(x^i,  z_1^{r_1}, c) 
        \land \ldots \land \flowsto(z_n^{r_n},  y^j, c).
  \]
   By induction on the trace $\trace$ we have the following cases:
  \begin{case}($\trace = {[]}$)
    \\
    Since for all $\event_x, \event_y \in \eventset^{\asn}$,
     $\nexists \trace' \in \ftdom$,satisfies $
    {[]}  = [\event_x] \tracecat \trace' \tracecat [\event_y]$, the theorem is vacuously true.
    \end{case}
    %
    \begin{case}($\event \in \eventset, \trace = [\event]$)
    \\
    Since for all $\event_x, \event_y \in \eventset^{\asn}$,
     $\nexists \trace' \in \ftdom$,satisfies $
    {[]} = [\event_x] \tracecat \trace' \tracecat [\event_y]$, the theorem is vacuously true.
    \end{case}
    %
    \begin{case}
      \label{case:soundness_basecase}
      ($\event_x', \event_y' \in \eventset $, $\trace = [\event_x'; \event_y']$)
      \\
      To show:
      % \wq{$\forall l \in \mathcal{A}(\event_x, \event_y, \trace, c, D)$ below?}
      %\jl{yes, there is a typo}
      \[
      \begin{array}{l}
        \forall \event_x, \event_y \in \eventset^{\asn} \sthat 
        \exists \trace' \in \ftdom \sthat  [\event_x'; \event_y'] = [\event_x] \tracecat \trace' \tracecat [\event_y]
        \\ \qquad 
            \implies    
        % \forall  z^i, y^j \in \lvar(c), l_h, l_t \sthat  
          \eventdep(\event_x, \event_y, [\event_x; \event_y], c, D)
         \implies \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)}, \pi_1(\event_y)^{\pi_2(\event_y)}, c)
      \end{array}
      \]
      %
      Taking arbitrary $ \event_x, \event_y \in \eventset^{\asn}$, by law of excluded middle, there are 2 cases:
      \\
      $\event_x = \event_x' \land  \event_y = \event_y'$
      \\
      $\neg(\event_x = \event_x' \land  \event_y = \event_y')$
      \\
      In case of $\neg(\event_x = \event_x' \land  \event_y = \event_y')$, since 
      $\nexists \trace' \in \ftdom$,satisfies $
      [\event_x'; \event_y'] = [\event_x] \tracecat \trace' \tracecat [\event_y]$, 
      the theorem is vacuously true.
      \\
      %
      In case of $\event_x = \event_x' \land  \event_y = \event_y'$,
      let $\trace' = []$, we know $\exists \trace' \in \ftdom$ satisfying 
      $[\event_x; \event_y] = [\event_x] \tracecat \trace' \tracecat [\event_y]$.
      \\
      % By Inversion Lemma~\ref{lem:inv_alg2}, we have either one of the two following cases:
      % \begin{enumerate}
      %   \item $\mathcal{A}(\event_x, \event_y, [\event_x; \event_y], c, D) = 
      %   \left\{[\pi_1(\event_x)^{\pi_2(\event_x)}, \pi_1(\event_y)^{\pi_2(\event_y)}] \right \}$ 
      %   and $\eventdep(\event_x, \event_y, \cdot  \event_x \cdot \event_y, c, D)$.
      %   \item  $\mathcal{A}(\event_x, \event_y, [\event_x; \event_y], c, D) = \{\}$ 
      %   and $\neg \eventdep(\event_x, \event_y, \cdot  \event_x \cdot \event_y, c, D)$;
      % \end{enumerate}
      % %
      % In case of $\mathcal{A}(\event_x, \event_y, [\event_x; \event_y], c, D) = \{\}$,
      % since $\nexists  z^i, y^j \in \lvar(c), l_h, l_t \sthat  l_h ++ [z^i, y^j] ++ l_t \in \{\}$, the theorem is vacuously true.
      % %
      % \\
      % Then in the case 1., 
      Then it is sufficient to show: 
      % \wq{Because $l \in A \implies DEP$ }
      % \jl{yes}
      \[
        \eventdep(\event_x, \event_y, [\event_x; \event_y], c, D) 
        \implies \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)}, \pi_1(\event_y)^{\pi_2(\event_y)}, c)
        % \land \eventdep(\event_x, \event_y, \cdot  \event_x \cdot \event_y, c, D)
      \]
      %
     By Lemma~\ref{lem:flowsto_soundness_emptytrace}, we have this case proved.
      %
      \end{case}
      %
        %
        \begin{case}
        ($\event_x', \event_y' \in \eventset$, $\trace_{ih} \in \ftdom, \trace = [\event_x'] \tracecat \trace_{ih} \tracecat [\event_y'] \land \trace_{ih} \neq {[]}$)
        \\
        It is sufficient to show:
        \[    \begin{array}{l}
         \forall \event_x, \event_y \in \eventset \sthat 
          \event_x, \event_y \in \eventset^{\asn} \land 
           \exists \trace' \in \ftdom \sthat  \trace = [\event_x] \tracecat \trace' \tracecat [\event_y]
           \\ \quad  
           \implies
          \eventdep(\event_x, \event_y, [\event_x'] \tracecat \trace_{ih} \tracecat [\event_y'], c, D) 
          \\ \quad 
          \implies 
          \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat  n \geq 0 
          \\ \quad  
          \land
          \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)},  z_1^{r_1}, c) 
          \land \ldots \land \flowsto(z_n^{r_n}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) 
        \end{array}
        \]
        %
        with the induction hypothesis:
        %
        \[
          \begin{array}{l}
            \forall \event_{ih1}, \event_{ih2} \in \trace \sthat  \event_{ih1}, \event_{ih2} \in \eventset^{\asn} 
            \land
             \exists \trace' \in \ftdom \sthat  
             \trace[\event_{ih1}:\event_{ih2}] = [\event_{ih1}] \tracecat \trace' \tracecat [\event_{ih2}]
             \\ \quad  
            \implies
            \eventdep(\event_{ih1}, \event_{ih2}, \trace[\event_{ih1}:\event_{ih2}], c, D) 
            \\ \quad 
            \implies 
            \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat  n \geq 0 \land
            \flowsto(\pi_1(\event_{ih1})^{\pi_2(\event_{ih1})},  z_1^{r_1}, c) 
            \\ \quad  
            \land \ldots \land \flowsto(z_n^{r_n}, \pi_1(\event_{ih2})^{\pi_2(\event_{ih2})}, c) 
          \end{array}
        \]
        %
        %
        Taking arbitrary $ \event_x, \event_y \in \eventset^{\asn}$, by law of excluded middle, there are 2 cases:
        \\
        $\event_x = \event_x' \land  \event_y = \event_y'$
        \\
        $\neg(\event_x = \event_x' \land  \event_y = \event_y')$
        \\
        In case of $\neg(\event_x = \event_x' \land  \event_y = \event_y')$, since 
        $\nexists \trace' \in \ftdom$,satisfies $
        [\event_x'] \tracecat \trace_{ih} \tracecat [\event_y']  = [\event_x] \tracecat \trace' \tracecat [\event_y]$, 
        the theorem is vacuously true.
        \\
        %
        In case of $\event_x = \event_x' \land  \event_y = \event_y'$,
        let $\trace' = \trace_{ih}$, we know $\exists \trace' \in \ftdom$ satisfying 
        $[\event_x'] \tracecat \trace_{ih} \tracecat [\event_y'] = [\event_x] \tracecat \trace' \tracecat [\event_y]$.
        \\
        To show:
        \[
          \begin{array}{l}
            \eventdep(\event_x, \event_y, [\event_x] \tracecat \trace_{ih} \tracecat [\event_y], c, D) 
          \\ \quad 
          \implies 
          \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat  n \geq 0 \land
          \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)},  z_1^{r_1}, c) 
          \\ \quad  
          \land \ldots \land \flowsto(z_n^{r_n}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) 
        \end{array}
        \]
        %
        By Lemma~\ref{lem:depevents_exist}, we know:
        \[
          \begin{array}{l}
            \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) 
        \\ \qquad \quad \lor
        \exists \event \in \trace_{ih} \sthat  
        % \left( 		
          \eventdep(\event_x, \event, \trace[\event_x:\event], c, D)
          \\ \quad  
          \land 
        \flowsto(\pi_1(\event)^{\pi_2(\event)}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) 
      \end{array}
      \]
        \\
        In first case, we have $\flowsto(\pi_1(\event_x)^{\pi_2(\event_x)}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) $ proved directly.
        \\
        In the second case, let $\event_{ih}$ be this event, from the induction hypothesis, we know:
        \[
          \begin{array}{l}
            \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) 
            \sthat  n \geq 0 
            \\ \quad  \land
            \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)},  z_1^{r_1}, c) 
            \land \ldots \land \flowsto(z_n^{r_n}, \pi_1(\event_{ih})^{\pi_2(\event_{ih})}, c) 
          \end{array}
          \]
          % \\
          Then we know:
          \[
            \begin{array}{l}
              \exists n \in \mathbb{N}, z_1^{r_1}, \ldots, z_n^{r_n} \in \lvar(c) \sthat  n \geq 0 \land
            \flowsto(\pi_1(\event_x)^{\pi_2(\event_x)},  z_1^{r_1}, c) 
            \\ \quad  
            \land \ldots \land \flowsto(z_n^{r_n}, \pi_1(\event_{ih})^{\pi_2(\event_{ih})}, c) )
             \\ \quad  
             \land  \flowsto(\pi_1(\event)^{\pi_2(\event)}, \pi_1(\event_y)^{\pi_2(\event_y)}, c) 
            \end{array}
            \]
            This case is proved.
      \end{case}

\end{proof}

\subsection{Inversion Lemmas and Helper Lemmas}
The following are the inversion lemmas and helper lemmas used in the proof of Theorem~\ref{thm:flowsto_event_soundness} above,
showing the correspondence properties between the trace based semantics and the program analysis results.
\input{appendices/thm-adaptivity/lem_depinversion}